# STATIC World Assets

Production world assets live here. Keep source files, provider exports, and licensed media out of the public deploy unless they are approved for web delivery.

Use optimized `.glb` or `.gltf` files for browser prototypes. Keep high-resolution source files in private storage until an asset pipeline is added.

