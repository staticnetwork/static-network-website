# Entity Assets

Production GLB/glTF assets belong in the category directories below. The current application uses a procedural Three.js development stand-in and never represents it as a final character model.

